1.The  IAM policy grants programmatic read-write access to the test bucket on s3
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["s3:ListBucket"],
      "Resource": ["arn:aws:s3:::test"]
    },
    {
      "Effect": "Allow",
      "Action": [
        "s3:PutObject",
        "s3:GetObject",
        "s3:DeleteObject"
      ],
      "Resource": ["arn:aws:s3:::test/*"]
    }
  ]
}

2.What will be the outcome of this IAM Policy?
{
"Version": "2012-10-17",
"Statement": [{
"Action": "ec2:*",
"Resource": "*",
"Effect": "Allow"
},
{
"Action": "*",
"Resource": "*",
"Effect": "Deny"
}
]
}

ans:

the policy allow an administrator to access to all the resourses and perform actions of ec2

3.steps to launch ec2 instance

1. go to  AWS Management Console
2. open ec2 console under Compute
3.from ec2 dashboard choose Launch Instance.
4. choose AMI (amazon machine image)
5.On the Choose an Instance Type page choose t.2 micro(hardware config)
6. choose launch on the review instance launch page
7. create key pair by clicking Create a new key pair.
8. save the private key.
9.choose launch instance.

4.1.Create an Amazon S3 Bucket with a unique name
2.configure options and set permissions.
3.enable static website hosting on the bucket.
4.upload static website code to the bucket.
5.access the static website using custom domain name using route 53.
6.enable url redirection,by creating another bucket in same way and add protocol as http.

5.VPS details:

VPC Name: MyVPC
CIDR : 10.0.0.0/16
Public Subnet: 10.0.1.0/24
Private Subnet: 10.0.2.0/24

1.create vpc (login to aws management console and navigate to vpc console)
2.enter new vpc cidr details and name and 10.0.0.0/16 as ipv4 cidr block click on create.
3.create public subnet under subnets option and enter 10.0.1.0/24 in “IPv4 CIDR block” for public subnet.
4. create private subnet with  CIDR 10.0.2.0/24
5.create and attach INTERNET GATEWAY.
6.attach to vpc.
7.add route to public subnet by creating route table
8. edit and Add “0.0.0.0/0” as destination.
9.associate with public subnet.

